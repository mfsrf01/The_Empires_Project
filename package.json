{
  "name": "replicator",
  "version": "0.1.0",
  "private": true,
  "description": "Foundational simulation for a procedural space resource game.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn src/server.ts"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/node": "^20.11.5",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
